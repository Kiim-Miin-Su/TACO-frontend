import { AttendanceBookView } from "@/features/attendance/AttendanceBookView";

// 출석부(LMS형) — 회차×학생 매트릭스 + 누적 시수(피드백 2026-07-03)
export default function AttendancePage() {
  return <AttendanceBookView />;
}
